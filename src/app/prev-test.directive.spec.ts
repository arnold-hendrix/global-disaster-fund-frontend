import { PrevTestDirective } from './prev-test.directive';

describe('PrevTestDirective', () => {
  it('should create an instance', () => {
    const directive = new PrevTestDirective();
    expect(directive).toBeTruthy();
  });
});
