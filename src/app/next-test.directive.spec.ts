import { NextTestDirective } from './next-test.directive';

describe('NextTestDirective', () => {
  it('should create an instance', () => {
    const directive = new NextTestDirective();
    expect(directive).toBeTruthy();
  });
});
